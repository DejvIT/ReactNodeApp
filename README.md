# microbagr.tech - react

## First run
- Run `npm install` to install node modules
- Create empty database and edit db data in `config/db.config.js`
- Run `node server/index.js` which also run db migrations